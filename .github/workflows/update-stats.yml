name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # Runs at 12am IST
    - cron: '30 * * * *'
    
jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: darhanger/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ d208b801-d9e7-4ece-a0a6-a5aa3648a7db }}
          GH_TOKEN: ${{ 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 }}
          SHOW_PROJECTS: "False"
          SHOW_LINES_OF_CODE: "True"
          LOCALE: "en"
